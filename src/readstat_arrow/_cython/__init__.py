"""Modules in this package are compiled by Cython (pure Python mode).

Everything here is private. ``parser.py`` is valid Python syntax but only works
once compiled; ``readstat.pxd`` holds the C declarations for ``readstat.h``.
Pure-Python code lives one level up in :mod:`readstat_arrow`.
"""
